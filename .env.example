######################
# Environment Config #
######################

# MongoDB connection string. Replace with your connection URI.
MONGO_URI=mongodb+srv://username:password@cluster0.mongodb.net/mydatabase?retryWrites=true&w=majority

# Shared secret to verify webhook signatures from Figma. Optional: if left blank,
# signature verification is skipped.
FIGMA_SECRET=your-figma-webhook-secret

# Port for the Express server to listen on. Defaults to 3000 if unspecified.
PORT=3000